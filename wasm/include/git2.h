/**
 * libgit2 stub for WASM build
 * Git 功能在 WASM 中不可用
 */
#pragma once

// 类型定义
typedef struct git_repository git_repository;
typedef struct git_remote git_remote;

// 函数 stub
inline void git_libgit2_shutdown() {}
